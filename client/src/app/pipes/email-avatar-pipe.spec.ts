import { EmailAvatarPipe } from './email-avatar-pipe';

describe('EmailAvatarPipe', () => {
  it('create an instance', () => {
    const pipe = new EmailAvatarPipe();
    expect(pipe).toBeTruthy();
  });
});
