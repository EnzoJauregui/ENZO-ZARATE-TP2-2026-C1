import { CambiarBorde } from './cambiar-borde';

describe('CambiarBorde', () => {
  it('should create an instance', () => {
    const directive = new CambiarBorde();
    expect(directive).toBeTruthy();
  });
});
