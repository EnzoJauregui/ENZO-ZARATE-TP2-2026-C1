import { ZoomMouse } from './zoom-mouse';

describe('ZoomMouse', () => {
  it('should create an instance', () => {
    const directive = new ZoomMouse();
    expect(directive).toBeTruthy();
  });
});
