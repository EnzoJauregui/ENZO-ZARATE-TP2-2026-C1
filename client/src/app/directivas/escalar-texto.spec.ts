import { EscalarTexto } from './escalar-texto';

describe('EscalarTexto', () => {
  it('should create an instance', () => {
    const directive = new EscalarTexto();
    expect(directive).toBeTruthy();
  });
});
